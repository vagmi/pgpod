//! SQLite state store for pgpod
//!
//! Not yet implemented — landing in **Phase 1** (see `ROADMAP.md`). The
//! crate exists now so the workspace shape from `adrs/00-project-setup.md`
//! §2 is real rather than aspirational, and so dependency direction is
//! enforced by the compiler from the first commit.
