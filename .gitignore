/target
/references/*/target

references
# Test VM artifacts — the base cloud image is ~800M and the per-VM
# overlay is generated.
/ops/testvm/images/
/ops/testvm/seed*.iso
/ops/testvm/user-data

# Release staging, built by .github/workflows/release.yml
/dist/

# Local overrides
/.pgpod-home/
